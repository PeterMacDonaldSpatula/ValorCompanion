package petermacdonald.valorgmcompanion.character.enums;

/**
 * This is an enum of possible core types. BARRIER_BLAST is the Barrier core keyed to a Blast Radius shape, and BARRIER_LINE is the Barrier core keyed to a Line shape.
 */

public enum CoreType {
    BARRIER_BLAST, BARRIER_LINE, BOOST, DAMAGE, HEALING, MIMIC, SUMMONING, WEAKEN, ULTIMATE_ATTACK, ULTIMATE_TRANSFORMATION
}
