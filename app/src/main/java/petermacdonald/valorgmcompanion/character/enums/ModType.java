package petermacdonald.valorgmcompanion.character.enums;

/**
 * This enumerated type describes what type of modifier this is. Used for determining if this is a special technique or not, dividing mods into types,a nd excluding certain types.
 */

public enum ModType {
    TARGET, SPECIAL, ALTERATION, ULTIMATE
}
